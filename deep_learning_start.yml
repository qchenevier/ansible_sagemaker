- hosts: localhost
  gather_facts: False
  roles:
    - VPC
    - EC2

- hosts: running
  gather_facts: False
  roles:
    - jupyter
    - role: ssh_tunnel
      instance_port: "{{ jupyter_port }}"
      local_port: "{{ jupyter_port }}"
